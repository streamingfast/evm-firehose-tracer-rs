# Sync Run: a96a677..c6eb9e2

## Overview

Port Golang commits from `a96a677` to `c6eb9e2` (inclusive).

## Commits to Port

| SHA | Message | Status |
|-----|---------|--------|
| `f26d2cb` | Fixed compilation on latest merge | Skipped (Go import path change only) |
| `6b7c51d` | Fixed a small inconsistency when building set code authorization signature r/s values | Done |
| `c6eb9e2` | Merge branch 'v4' | Done (merge commit, no new changes) |

## Work Items

### `6b7c51d` - Fixed set code authorization r/s values

**Bug**: SetCode authorization `r` and `s` fields were serialized as 32 zero bytes
when zero, instead of empty bytes (matching production native tracer behavior).

**Fix**: In `firehose/src/tracer.rs`, changed:
```rust
r: auth.r.0.to_vec(),
s: auth.s.0.to_vec(),
```
To:
```rust
r: utils::normalize_signature_point(&auth.r.0).unwrap_or_default(),
s: utils::normalize_signature_point(&auth.s.0).unwrap_or_default(),
```

**Test**: Added `test_zero_r_s_signature_serializes_as_empty` in `tracer_setcode_test.rs`.

**CHANGELOG**: Added entry to `## Unreleased` section.

### Additional: Registered missing tests in Cargo.toml

Added missing `[[test]]` entries that existed in the filesystem but were not registered:
- `tracer_genesis_test`
- `tracer_setcode_test`
- `tracer_delegation_test`

## Status: COMPLETED
